extends ../layout
block content
  article
    h1= article.title
    div
      - var date = new date(article.published).toISOString().slice(0, 19).replace('T', ' ');
      small Posted: #{date}
    div!= article.body
